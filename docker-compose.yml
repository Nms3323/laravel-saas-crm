version: '3.8'
services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./:/var/www/html
    depends_on:
      - db
  db:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: laravel_saas_crm
      MYSQL_ROOT_PASSWORD: secret
    ports:
      - "3306:3306"
